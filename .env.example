# JechSpace Waitlist Frontend Environment Variables

# API Configuration
# The base URL for the JechSpace API
# Development: use your local API server or staging environment  
# Production: use the production API endpoint (fix the domain name)
VITE_API_URL=https://api.jechspace.com/api/v1

# App Configuration
VITE_APP_NAME=JechSpace
VITE_APP_VERSION=1.0.0

# Development Configuration (optional)
NODE_ENV=production

# Analytics & Monitoring (optional - uncomment and configure as needed)
# VITE_ANALYTICS_ID=your_analytics_id
# VITE_SENTRY_DSN=your_sentry_dsn

# Feature Flags (optional)
# VITE_ENABLE_DEBUG=false
# VITE_ENABLE_ANALYTICS=true
